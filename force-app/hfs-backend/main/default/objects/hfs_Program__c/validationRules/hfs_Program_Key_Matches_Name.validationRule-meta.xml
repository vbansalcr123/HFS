<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>hfs_Program_Key_Matches_Name</fullName>
    <active>true</active>
    <description>Keeps the unique Program Key aligned with the Program Name so the uniqueness guard actually prevents duplicate program names.</description>
    <errorConditionFormula>LOWER(hfs_Program_Key__c) &lt;&gt; LOWER(Name)</errorConditionFormula>
    <errorDisplayField>hfs_Program_Key__c</errorDisplayField>
    <errorMessage>Program Key must match the Program Name (it is used to prevent duplicate programs).</errorMessage>
</ValidationRule>
